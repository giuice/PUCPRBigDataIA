# Execute os comandos abaixo no MySQL Workbench

# Cria o banco de dados
create database carros;

# Usa o banco de dados
use carros;

# Cria tabela
CREATE TABLE carrosTB (
	MAKE		 varchar(50),
	FUELTYPE	 varchar(50),   
	ASPIRE		 varchar(50),
	DOORS		 varchar(50),
	BODY		 varchar(50),
	DRIVE		 varchar(50),
	CYLINDERS	 varchar(50),
	HP		     varchar(50),		
	RPM		 	 varchar(50),
	MPGCITY	     varchar(50),
	MPGHWY	 	 varchar(50),
	PRICE		 varchar(50)
);	